{% include 'email/layout/header.html.twig' %}

{% if email_id is defined %}
    <img src="http://localhost:8000{{ path('tracking_pixel', {'email_id': email_id}) }}" alt="" style="width: 1px; height: 1px; display: none;">
{% endif %}
<div>
    <h3 style="font-size: 1.7rem">Compte Arkada</h3>
    <p>Un nouveau compte vient d'être créé pour l'adresse : {{ to }}. </p>
    <p>Un mot de passe vous a été envoyé pour pouvoir vous connecter avec votre adresse e-mail.</p>
    <p style="color: red; text-align: center"><br>Une fois connecté sur votre compte, n'oubliez pas de modifier votre mot de passe !</p>
    <p style="text-align: center; padding-top: 1rem">Mot de passe :</p>
    <div style="text-align: center">
        <span style=" font-size: 1.5rem; font-weight: bold">{{ password }}</span>
    </div>
</div>
<div style="display: flex; width: 100%; justify-content: center; padding-top: 1rem">
    <a class="button-callback" href="http://localhost:4200/#/login">
        Accéder au CRM
    </a>
</div>

<style>
    a.button-callback {
        padding: .5rem 1rem;
        background: linear-gradient(70deg, #B25E1E, #DF972C);
        color: black;
        border-radius: 10px;
        font-weight: bold;
        transition: all .3s;
        text-decoration: none;
        border: 2px solid #232225;
    }

    a:hover {
        background: transparent;
        border: 2px solid #B25E1E;
        color: #B25E1E;
    }
</style>

{% include 'email/layout/footer.html.twig' %}