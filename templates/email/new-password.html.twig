{% include 'email/layout/header.html.twig' %}

{% if email_id is defined %}
    <img src="http://localhost:8000{{ path('tracking_pixel', {'email_id': email_id}) }}" alt="" style="width: 1px; height: 1px; display: none;">
{% endif %}
<div>
    <h3 style="font-size: 1.7rem">Compte Arkada</h3>
    <p>Votre demande de réinitialisation de mot de passe a bien été prise en compte. </p>
    <p>Veuillez cliquer sur le lien suivant afin de continuer la procédure.</p>

</div>
<div style="display: flex; width: 100%; justify-content: center; padding-top: 1rem">
    <a class="button-callback" href="http://localhost:4200/#/reset-password/{{ token }}">
        Réinitialiser mon mot de passe
    </a>
</div>

<style>
    a.button-callback {
        padding: .5rem 1rem;
        background: linear-gradient(70deg, #B25E1E, #DF972C);
        color: black;
        border-radius: 10px;
        font-weight: bold;
        transition: all .3s;
        text-decoration: none;
        border: 2px solid #232225;
    }

    a:hover {
        background: transparent;
        border: 2px solid #B25E1E;
        color: #B25E1E;
    }
</style>

{% include 'email/layout/footer.html.twig' %}